name: IncidentLens Zero-Cost DEV v1 (consumed)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  consumed-dev-v1:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'DEV v1 task split is consumed for tuning.'
          echo 'Canonical run: 33396064815.'
          echo 'The original scorer had an MCQ reference bug; use scoring-corrected v1.1 for the same frozen adapter.'
