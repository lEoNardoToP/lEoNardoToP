name: IncidentLens Consumed Human Baseline

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  consumed-baseline:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'This workflow is intentionally manual-only.'
          echo 'The 61 same-task human-overlap tasks from run 33389075503 are CONSUMED and must not be used for tuning.'
          echo 'Canonical evidence artifact: incidentlens-zero-cost-command-human, run 33389075503.'
