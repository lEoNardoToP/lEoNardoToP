name: IncidentLens Zero-Cost DEV v1.1 (consumed)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  consumed-v11:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Scoring-corrected DEV v1.1 is consumed.'
          echo 'Canonical run: 33396631322.'
          echo 'Result: adapter improves DEV but regresses internal holdout; NOT PROMOTED.'
