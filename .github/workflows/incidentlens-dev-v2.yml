name: IncidentLens DEV v2 (frozen)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'DEV v2 is frozen after run 33397361564.'
          echo 'Holdout exact: 13/40 vs baseline 11/40 = +5.0 percentage points.'
          echo 'The artifact boolean was false only because floating arithmetic encoded the gain as 4.999999999999999.'
          echo 'No further tuning on this split is permitted.'
